name: Celebrate MCP Success

on:
  workflow_run:
    workflows: ["mcp-validation"]
    types: ["completed"]

jobs:
  celebrate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Celebrate MCP success
        run: |
          echo "🎉 MCP validation passed for run ${{ github.event.workflow_run.id }}"
