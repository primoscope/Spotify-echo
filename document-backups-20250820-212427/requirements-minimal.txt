# EchoTune AI - Core Production Requirements (Minimal)
# Only essential dependencies for core functionality
# Excludes: Heavy ML packages, dev tools, coding agent dependencies

# Core API Integration
spotipy>=2.22.0
requests>=2.31.0

# Async and WebSocket Support  
aiohttp>=3.8.0
websockets>=11.0.0

# Database Support (minimal)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pymongo>=4.6.0

# Environment Management
python-dotenv>=1.0.0

# Basic Web Framework
fastapi>=0.100.0
uvicorn>=0.22.0

# Logging
structlog>=23.0.0

# Progress indicators
tqdm>=4.65.0

# Note: Heavy ML packages explicitly excluded for minimal footprint:
# - pandas (heavy data processing)
# - numpy (large numerical library) 
# - scikit-learn (large ML package)
# - matplotlib, seaborn, plotly (visualization libraries) 
# - jupyter, ipython (notebook dependencies)
# - librosa (heavy audio processing)
# - pytest-cov, black, mypy (dev tools)
# - All MCP server dependencies
# - Browser automation tools (puppeteer, playwright)

# For data analysis features, install these separately:
# pip install pandas numpy scikit-learn matplotlib