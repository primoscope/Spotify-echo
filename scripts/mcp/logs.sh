#!/usr/bin/env bash
set -euo pipefail

echo "Logs are emitted by your MCP client (e.g., Claude Desktop) and the servers it launches."
echo "Check the client dev logs or launch servers manually to tail their stdout/stderr."